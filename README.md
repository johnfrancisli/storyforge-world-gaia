# PLACEHOLDER World Name

One line a player sees before their first turn.

This folder is a self-contained Storyforge world. Canon lives in `world.json`, `rules/`, `lore/`, and `assets/`. The `campaigns/` directory is local play data and is intentionally not versioned.
